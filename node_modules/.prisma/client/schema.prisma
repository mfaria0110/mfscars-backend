generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Empresa {
  id       Int     @id @default(autoincrement())
  nome     String
  cidade   String
  estado   String
  email    String?
  telefone String?
}
